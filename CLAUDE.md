# Claude Code project handoff

Start by reading `CLAUDE_HANDOFF.md`, `AGENTS.md`, and `ARCH_PLAN.md`.

Read the latest files directly from the shared project folder; no chat uploads are needed. Consult `IMPLEMENTATION_TIMELINE.md` for milestone order and responsibilities, and update it when a milestone completes or the sequence changes. No separate changelog is required.

Claude's assigned role is architecture, security and logic review; Codex's assigned role is implementation and verification. Follow the user's latest instructions when responsibilities change.

After **every completed work item**, update `CLAUDE_HANDOFF.md` with what changed, relevant files, checks actually performed, remaining issues, and concrete next actions with an owner. Do this throughout the work, not only at the end of a session. Preserve unresolved issues and distinguish prototype results from production verification. Never put credentials or raw customer/card data in the handoff.

Keep the `## Copy-ready prompt for Claude Code` section at the end of `CLAUDE_HANDOFF.md` current. When Codex leaves a prompt there, treat it as the next requested review. When you finish, replace it with the single paste-ready prompt that describes Codex's next task.
